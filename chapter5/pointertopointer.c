#include <stdio.h>
int main(){
    // int a = 10;
    // int *p = &a; // p stores the address of a
    // int **q; // q stores the address of p  double indirection pointer
    // q = &p;

    // printf("\n a is %d and &a is %u", a, &a);  // %u is used for address
    // printf("\n p is %u and &a is %u, *p is %d", p, &p,*p);
    // printf("\n q is %u and &p is %u, **q is %d", q, &q, **q);




    //Null pointer
    // int *ptr = NULL; // ptr is a null pointer       or          int *ptr = 0;
    // printf("\n ptr is %u", ptr);



    // pointer
}
