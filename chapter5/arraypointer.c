#include <stdio.h>

int main() {
    int a[] = {1, 2, 3};
    printf("%d", 2[a]);


    //  int a[5] = {10, 20, 30, 40, 50};
    // int *p = &a[4];


    // for (int i = 4; i >= 0; i--) {
    //     printf("Val: %d\tAddr: %u\n", *p, p);
    //     p--;  //p=p-1z
    // }





    // int a[5] = {1, 5, 7, 19, 20};
    // int *p = a;
    // printf("%d", *p++);

    // p=&a[0];
    // printf("%u", p);
    // printf("\n%u", &a[0]);
    // printf("\n%u", a);

    // printf("%u", a);
    // printf("\n%u", &a[0]);
    // printf("\n%u", &a[1]);

    // printf("\na = %p %u\n", a,a);          // Address of a[0]
    // printf("&a[0] = %p %u\n", &a[0],&a);


    // printf("\n%d", *(a+1));
    // printf("\n%d", *(a+2));
    // printf("\n%d", *(a+3));
    // printf("\n%d", a[2]);

    // printf("\n%u", a+1); // isse khali address print hoga %d nhi aayega
    // printf("\n%u", a+2);
    // printf("\n%u", a+3);


    // for (int i = 0; i < 5; i++) {
    //     printf("a[%d] = %d\n", i, *(a + i));
    // }

}
